version: "1"
name: "Invalid Name With Spaces!"

source:
  type: kafka_cdc
  topic: some.topic
  bootstrap_servers: localhost:9092

sink:
  type: kafka
  topic: out.topic
  bootstrap_servers: localhost:9092

# hobby-session-25

# hobby-session-331
